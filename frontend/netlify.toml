# Netlify configuration for HR Management System frontend
# This file tells Netlify how to build and serve the React/Vite app

[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — all routes go to index.html so React Router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
